spring.application.name=appointment-management-system
server.port=8084

spring.datasource.url=jdbc:mysql://localhost:3306/appointments_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.cloud.compatibility-verifier.enabled=false
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.format_sql=true

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/
# spring.cloud.discovery.enabled=true # Generally enabled by default if client is on classpath and URL is set

# Security (if basic auth is used, otherwise configure properly with Spring Security)
# spring.security.user.name=admin
# spring.security.user.password=admin
# spring.security.enabled=true # Set to false if not using basic auth and relying on other security mechanisms

# Kafka - Add if this service needs to produce/consume Kafka messages
# spring.kafka.producer.bootstrap-servers=kafka:9092
# spring.kafka.consumer.bootstrap-servers=kafka:9092
# spring.kafka.consumer.group-id=appointment-group
